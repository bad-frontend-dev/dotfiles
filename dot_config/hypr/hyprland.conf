animations {
	bezier = easeOutQuint,0.23,1,0.32,1
	bezier = easeInOutCubic,0.65,0.05,0.36,1
	bezier = linear,0,0,1,1
	bezier = almostLinear,0.5,0.5,0.75,1.0
	bezier = quick,0.15,0,0.1,1

	animation = global, 1, 10, default
	animation = border, 1, 5.39, easeOutQuint
	animation = windows, 1, 4.79, easeOutQuint
	animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
	animation = windowsOut, 1, 1.49, linear, popin 87%
	animation = fadeIn, 1, 1.73, almostLinear
	animation = fadeOut, 1, 1.46, almostLinear
	animation = fade, 1, 3.03, quick
	animation = layers, 1, 3.81, easeOutQuint
	animation = layersIn, 1, 4, easeOutQuint, fade
	animation = layersOut, 1, 1.5, linear, fade
	animation = fadeLayersIn, 1, 1.79, almostLinear
	animation = fadeLayersOut, 1, 1.39, almostLinear
	animation = workspaces, 1, 1.94, almostLinear, fade
	animation = workspacesIn, 1, 1.21, almostLinear, fade
	animation = workspacesOut, 1, 1.94, almostLinear, fade
} 

exec-once = waybar
exec-once = hyprpaper
exec-once = dunst

exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

bind = CTRL ALT, T, exec, alacritty 
bind = SUPER SHIFT, S, exec, hyprshot -m region -z
bind = SUPER, V, exec, cliphist list | wofi --show dmenu | cliphist decode | wl-copy

bind = SUPER, 1, workspace, 1
bind = SUPER, 2, workspace, 2
bind = SUPER, 3, workspace, 3
bind = SUPER, 4, workspace, 4
bind = SUPER, 5, workspace, 5
bind = SUPER, 6, workspace, 6
bind = SUPER, 7, workspace, 7

 

bind = SUPER, 1, workspace, 1
bind = SUPER, 2, workspace, 2
bind = SUPER, 3, workspace, 3
bind = SUPER, 4, workspace, 4
bind = SUPER, 5, workspace, 5
bind = SUPER, 6, workspace, 6
bind = SUPER, 7, workspace, 7

bind = SUPER SHIFT, 1, movetoworkspace, 1
bind = SUPER SHIFT, 2, movetoworkspace, 2
bind = SUPER SHIFT, 3, movetoworkspace, 3
bind = SUPER SHIFT, 4, movetoworkspace, 4
bind = SUPER SHIFT, 5, movetoworkspace, 5
bind = SUPER SHIFT, 6, movetoworkspace, 6
bind = SUPER SHIFT, 7, movetoworkspace, 7

bind = ALT, TAB, workspace, m+1
bind = SHIFT ALT, TAB, workspace, m-1

bind = SUPER, LEFT, focusmonitor, DP-1
bind = SUPER, RIGHT, focusmonitor, DP-2

bind = SUPER, UP, movefocus, l
bind = SUPER, DOWN, movefocus, r

bind = SUPER SHIFT, LEFT, movewindow, mon:DP-1
bind = SUPER SHIFT, RIGHT, movewindow, mon:DP-2

bind = SUPER, R, swapnext
bind = ALT, RETURN, fullscreen, 0

bind = SUPER SHIFT, BACKSPACE, exec, hyprlock & systemctl suspend
bind = SUPER, BACKSPACE, exec, hyprlock 

bindm = SUPER, mouse:272, movewindow
bindm = SUPER, mouse:273, resizewindow

bindr = SUPER SHIFT, W, forcekillactive,
bindr = SUPER, W, killactive,
bindr = SUPER, D, exec, wofi --show drun

bind = SUPER, P, exec, librewolf
bind = SUPER SHIFT, P, exec, librewolf --private-window

bind = SUPER, T, exec, xfe

bind = SUPER, F, togglefloating,

bind = SUPER, C, exec, hyprpicker -a

#bind = ALT, TAB, workspace, empty
#bind = SHIFT ALT, TAB, workspace, previous

bind = , XF86AudioPlay, exec, playerctl play-pause
bind = , XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.5 @DEFAULT_AUDIO_SINK@ 2%+
bind = , XF86AudioLowerVolume, exec, wpctl set-volume -l 0 @DEFAULT_AUDIO_SINK@ 2%-

decoration {
	rounding = 15
	
	shadow {
		enabled = false
		range = 6
		offset = 5, 5
		color = rgb(000000)
	}

	blur {
		enabled = true
	}
}

windowrulev2 = noblur, class:^()$,title:^()$
windowrulev2 = move 0 0, title:(Wine System Tray)
windowrulev2 = float, title:(Picture-in-Picture)
windowrulev2 = tile, title:(Ubisoft Connect)

windowrulev2=move 0 0,class:(flameshot),title:(flameshot)
windowrulev2=pin,class:(flameshot),title:(flameshot)
windowrulev2=fullscreenstate,class:(flameshot),title:(flameshot)
windowrulev2=float,class:(flameshot),title:(flameshot)

env = GBM_BACKEND,nvidia-drm
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = LIBVA_DRIVER_NAME,nvidia
env = XDG_CURRENT_DESKTOP,Hyprland

general {
  gaps_out = 10
  resize_on_border = true
	col.active_border = rgb(cad3f5) rgb(b7bdf8)
	col.inactive_border = rgb(363a4f)
	border_size = 2
	extend_border_grab_area = 20
}

# render {
# 	explicit_sync = 1
# 	explicit_sync_kms = 0
# }

input {
	kb_layout = us,de,es
	kb_options = grp:win_space_toggle
}

misc {
	disable_splash_rendering = true
	disable_hyprland_logo = true
}

monitor = HDMI-A-1, 1280x1024@60, 0x0, 1
monitor = DP-1, 1920x1080@165, 1280x0, 1
monitor = DP-2, 1920x1080@165, 3200x0, 1


workspace = 1, monitor:DP-1, persistent:true
workspace = 2, monitor:DP-1, persistent:true
workspace = 3, monitor:DP-1, persistent:true
workspace = 4, monitor:DP-2, persistent:true
workspace = 5, monitor:DP-2, persistent:true
workspace = 6, monitor:DP-2, persistent:true
